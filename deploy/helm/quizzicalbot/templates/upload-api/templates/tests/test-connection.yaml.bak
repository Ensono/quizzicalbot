apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "uploadApi.fullname" . }}-test-connection"
  labels:
    {{- include "uploadApi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "uploadApi.fullname" . }}:{{ .Values.uploadApi.service.port }}']
  restartPolicy: Never
