apiVersion: v1
kind: Service
metadata:
  name: {{ include "questionApi.fullname" . }}
  labels:
    {{- include "questionApi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "questionApi.selectorLabels" . | nindent 4 }}
